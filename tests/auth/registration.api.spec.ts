import { test } from '@playwright/test';

test.describe('User Auth - Registration API', () => {
  test.skip('Registration flow will be automated after OTP/payload confirmation', async () => {
    // Need registration required payload + OTP source.
  });
});